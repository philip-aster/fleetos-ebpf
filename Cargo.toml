[workspace]
members = [
    "fleetos-ebpf-common",
    "ebpf",
    "xtask",
]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
